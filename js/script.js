console.log("Javascript up and running!");

const quotes = [
  {
    quote: "If you want something done right, do it yourself.",
    author: "Herman Bang",
    tags: ["productivity", "quality", "you"],
  },
];

/*const createParagraph = (target, source) => {
  let paragraph = document.createElement(source);

  let tags = quotes.forEach((element) => {
    paragraph.innerHTML = element.tags[0];
  });

  document.getElementById(target).appendChild(paragraph);
};

document.getElementById("body").onload = function () {
  createParagraph("tag", "P");
};*/
